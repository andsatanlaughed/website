{
  "name": "andsatanlaughed.com",
  "version": "1.0.0",
  "private": true,
  "description": "Static site for andsatanlaughed.com — self-hosted Tailwind + fonts.",
  "scripts": {
    "build": "tailwindcss -i ./src/input.css -o ./style.css --minify",
    "watch": "tailwindcss -i ./src/input.css -o ./style.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.17"
  }
}
